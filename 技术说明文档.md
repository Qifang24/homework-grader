# AI 作业批改系统 · 技术说明文档

> 朱批二人组 · 希沃智教π 命题赛道（赛题三）
> 关键词：多学科批改 · 手写识别 · 过程分 · 个性化评语 · 学情诊断

---

## 一、系统概述

本系统是一套**多学科 AI 作业批改系统**，输入学生作业图片，输出：逐题判分（含过程分）、个性化评语、薄弱知识点与班级学情报告，并支持教师一键复核。

- **覆盖学科**：数学、语文、英语（客观题）+ 语文作文、英语作文（四维度评分），满足"至少 2 个学科"要求。
- **两种批改模式**：
  - **自主批改**：仅凭一张学生图片，模型自行识别并判分，可给出**过程分**（赛题要求③）。
  - **对照标答**：同时输入学生页与教师答案页，逐题比对，**准确率最高**，用于客观题。
- **技术栈**：Python + Streamlit（前端）+ 智谱 GLM-4.6v 多模态大模型（temperature=0.1 保证可复现）。

---

## 二、整体架构

```
作业图片
  │  ① 图像预处理 (image_utils)
  ▼
┌─────────────── 批改引擎 ───────────────┐
│  自主批改(两阶段)        对照标答          │
│  转录→判分(math_grader)  比对(answer_key)  │
│  作文四维(essay_grader)                    │
└────────────────────────────────────────┘
  │  ② 结构化结果：分数 / 逐题对错+过程分 / 评语 / 薄弱知识点
  ▼
历史库(SQLite) · 班级学情报告 · 教师人工复核
```

核心模块：`image_utils`（预处理）、`math_grader`（数学两阶段）、`answer_key_grader`（对照标答）、`essay_grader`（作文四维）、`knowledge_points`（知识建模）、`database`（历史/复核）。

---

## 三、OCR 方案（赛题要求②：手写体识别）

**挑战**：学生手写的公式、汉字、英文，笔迹潦草、分数/根号/正负号等细节多。

**关键诊断**：我们用对照实验发现——模型的**瓶颈在"认手写"而非"解题"**。在自主批改中，模型常常自己算对了答案（`my_computed_answer` 正确），却把学生写的答案**读错**（如 `40√3` 读成 `4√3`、`±1/7` 读成 `2`、`1/(x+1)` 读成 `1/x`），从而误判。这一发现决定了我们的优化方向集中在识别端。

**四项措施**：

1. **图像预处理保真**：长边统一缩放到 3072px、JPEG 质量 92（`image_utils.py`）。压缩过狠会糊掉分数线、根号等细笔画，直接拉低识别率，故采用相对高的分辨率与画质。

2. **两阶段"先转录、再判分"**（数学核心创新）：
   - **阶段①·转录**：只做忠实 OCR，按题型（选择/填空/解答）逐题把学生写的步骤与最终答案完整转录，不判对错、不替学生改对。
   - **阶段②·判分**：以阶段①的转录为权威、不再重认笔迹，独立演算后逐题判分，并识别过程分。
   - 这样把"认字"与"算题"两个任务**解耦**，避免一次调用中互相挤占，识别误读显著下降。

3. **对照标答模式**：有教师答案页时，以标准答案为锚点，识别与判定双重校验，是准确率最高的模式（见第六节）。

4. **人机协同纠错**：识别/判分难免有误差，系统支持教师**一键修正分数、评语、姓名**（`database.update_review`），并保留 AI 原始结果备查（"AI 原评 → 教师修正"）。

---

## 四、知识建模（赛题要求⑤：知识点薄弱分析）

- **课标知识点体系**（`knowledge_points.py`）：为每个学科预设一套固定的知识点列表（数学覆盖中小学主要模块，作文按写作维度），避免大模型自由发挥导致无法聚合。
- **归一化对齐**（`normalize`）：模型批改时必须把每道错题归类到固定列表中的某一项；对模型输出做精确 + 模糊匹配，对齐到课标知识点，对不上则归入"其他"，保证统计不丢数据。
- **个人薄弱点**：把答错 / 部分正确题目的知识点去重，作为该生薄弱点。
- **班级学情报告**：批量批改后按知识点聚合，输出薄弱知识点排行，定位班级共性问题。

---

## 五、评语生成策略（赛题要求④：个性化评语）

- **个性化、反模板**：Prompt 强制评语**引用学生本人写的具体数字 / 步骤 / 句子**，明确禁止套话。
- **过程分**：答案错但解题过程有正确步骤时标记 `partial_credit`，给予过程分（如方法对、仅某步算错）。
- **逐题诊断**：每道错题输出第一处出错步骤（`error_step`）与原因（`error_reason`）。
- **结构化输出**：统一以 JSON 返回分数、逐题结果、评语（comment）、改进建议（suggestions）；作文额外输出主题/结构/语言/内容四维评分与亮点句。
- **可复现**：低 temperature（0.1）降低同一份作业的判定随机性。

---

## 六、准确率评测

**真值（ground truth）由人工核对教师答案页 + 学生页逐题判定，独立于系统任何模型输出**；评测脚本 `_accuracy_report.py` 可一键复现，真值标注见 `eval/gold/`。

### 对照标答模式（数学 + 英语客观题，共 136 个作答点）

| 学科 | 作答点 | 逐题判对率 | 分数 MAE |
| --- | --- | --- | --- |
| 数学 | 50 | 94.0% | 5.0 |
| 英语 | 86 | 98.8% | 1.0 |
| **合计** | **136** | **97.1%** | — |

### 两阶段改进效果（数学自主批改）

| | 逐题判对率 | 分数 MAE |
| --- | --- | --- |
| 单阶段（改进前） | 76.4% | 22.0 |
| 两阶段（改进后） | 78.3% | **17.8（↓19%）** |

并修复多处手写误读：`40√3→4√3`、`±1/7`、多值填空漏读等。

### 能力边界（如实说明）

- **对照标答（客观题）准确率最高**，是推荐的主用模式。
- **自主批改**仍受手写识别误差影响，存在偶发"假错"（把对的判成错）——故提供对照标答模式与教师复核兜底。
- **语文主观简答题**：因答案开放、"对错"为主观判断，二元比对偏严，分数仅作参考，由教师复核定分。

---

## 七、创新点

1. **两阶段"先转录、再判分"**：基于"瓶颈在识别非解题"的实证诊断，把 OCR 与判分解耦，针对性提升准确率。
2. **人机协同**：AI 给参考判定与逐题分析，教师一键复核修正，AI 辅助而非取代教师。
3. **知识点建模 → 学情诊断**：从单题对错上升到个人/班级薄弱知识点画像。
4. **离线评测脚手架**：以人工真值客观度量准确率，结论可复现、可追问。

---

## 八、运行方式

```bash
pip install -r requirements.txt
# 在 .env 中配置 ZHIPUAI_API_KEY
streamlit run app.py
```
